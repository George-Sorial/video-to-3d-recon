# SLAM / monocular depth + fusion pipeline.
#
# Install PyTorch FIRST with the CUDA build matching your driver, e.g. for CUDA 12.1:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# then:
#   pip install -r slam/requirements.txt
#
# COLMAP itself is a separate native install (not pip) — download the CUDA
# Windows build from https://github.com/colmap/colmap/releases and put it on PATH.

# Core geometry / IO
numpy
scipy
opencv-python
open3d                  # TSDF fusion, mesh IO, visualization
pillow
tqdm

# Camera poses
pycolmap                # used for model summaries + picking the largest model

# Monocular depth (Depth Anything V2 via HuggingFace)
transformers
timm
# torch / torchvision installed separately (see note above)

# Optional visualization
# rerun-sdk
